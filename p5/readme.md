### install:
only uses the p5 library in [assets/libraries-and-scripts](./assets/libraries-and-scripts/p5/) folder.

---
### preview locally: 
local server preview. .js files draw to an html-canvas, which can be loaded on a browser with index.html. 